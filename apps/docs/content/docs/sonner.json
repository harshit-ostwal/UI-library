{
    "title": "Sonner",
    "description": "An opinionated toaster component for rendering toast notifications.",
    "component": "Toaster",
    "installation": {
        "cli": "npx shery-ui add sonner",
        "manual": [
            {
                "title": "Install dependencies",
                "description": "Install the required dependencies for the sonner component.",
                "command": "npm install sonner lucide-react"
            },
            {
                "title": "Add utility function",
                "description": "Create a cn utility function if you don't have one already.",
                "filePath": "lib/utils.js",
                "code": "import { clsx } from 'clsx';\nimport { twMerge } from 'tailwind-merge';\n\nexport function cn(...inputs) {\n  return twMerge(clsx(inputs));\n}",
                "language": "javascript"
            }
        ]
    },
    "usage": {
        "import": "import { Toaster } from '@shery-ui/components';",
        "basic": "sonner-demo"
    },
    "examples": [
        {
            "title": "Default",
            "description": "A basic sonner example.",
            "demo": "sonner-demo",
            "code": "import { Button, Toaster } from \"@shery-ui/components\";\n\nexport default function SonnerDemo() {\n  return (\n    <div className=\"flex flex-col items-center gap-3\">\n      <Toaster />\n      <Button variant=\"outline\">Toaster mounted</Button>\n    </div>\n  );\n}"
        }
    ]
}
