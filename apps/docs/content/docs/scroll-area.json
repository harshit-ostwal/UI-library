{
    "title": "Scroll Area",
    "description": "A custom scrollable area with styled scrollbars.",
    "component": "ScrollArea",
    "installation": {
        "cli": "npx shery-ui add scroll-area",
        "manual": [
            {
                "title": "Install dependencies",
                "description": "Install the required dependencies for the scroll area component.",
                "command": "npm install @radix-ui/react-scroll-area"
            },
            {
                "title": "Add utility function",
                "description": "Create a cn utility function if you don't have one already.",
                "filePath": "lib/utils.js",
                "code": "import { clsx } from 'clsx';\nimport { twMerge } from 'tailwind-merge';\n\nexport function cn(...inputs) {\n  return twMerge(clsx(inputs));\n}",
                "language": "javascript"
            }
        ]
    },
    "usage": {
        "import": "import { ScrollArea } from '@shery-ui/components';",
        "basic": "scroll-area-demo"
    },
    "examples": [
        {
            "title": "Default",
            "description": "A basic scroll area example.",
            "demo": "scroll-area-demo",
            "code": "import { ScrollArea } from \"@shery-ui/components\";\n\nconst tags = Array.from({ length: 24 }, (_, index) => \"Tag \" + (index + 1));\n\nexport default function ScrollAreaDemo() {\n  return (\n    <ScrollArea className=\"h-48 w-72 rounded-lg border p-4\">\n      <div className=\"space-y-2\">\n        {tags.map((tag) => (\n          <div key={tag} className=\"rounded-md bg-muted px-3 py-2 text-sm\">{tag}</div>\n        ))}\n      </div>\n    </ScrollArea>\n  );\n}"
        }
    ]
}
