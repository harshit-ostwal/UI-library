{
    "title": "Tooltip",
    "description": "A small popup that appears when an element receives hover or focus.",
    "component": "Tooltip",
    "installation": {
        "cli": "npx shery-ui@latest add tooltip",
        "manual": [
            {
                "title": "Install dependencies",
                "description": "Install the required dependencies for the tooltip component.",
                "command": "npm install @radix-ui/react-tooltip"
            },
            {
                "title": "Add utility function",
                "description": "Create a cn utility function if you don't have one already.",
                "filePath": "lib/utils.js",
                "code": "import { clsx } from 'clsx';\nimport { twMerge } from 'tailwind-merge';\n\nexport function cn(...inputs) {\n  return twMerge(clsx(inputs));\n}",
                "language": "javascript"
            }
        ]
    },
    "usage": {
        "import": "import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@shery-ui/components';",
        "basic": "tooltip-demo"
    },
    "examples": [
        {
            "title": "Default",
            "description": "A basic tooltip example.",
            "demo": "tooltip-demo",
            "code": "import { Button, Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from \"@shery-ui/components\";\n\nexport default function TooltipDemo() {\n  return (\n    <TooltipProvider>\n      <Tooltip>\n        <TooltipTrigger asChild>\n          <Button variant=\"outline\">Hover</Button>\n        </TooltipTrigger>\n        <TooltipContent>\n          <p>Add to library</p>\n        </TooltipContent>\n      </Tooltip>\n    </TooltipProvider>\n  );\n}"
        }
    ]
}
