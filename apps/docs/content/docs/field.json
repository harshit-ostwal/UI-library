{
    "title": "Field",
    "description": "Field primitives for labels, descriptions, validation, and grouped form layout.",
    "component": "Field",
    "installation": {
        "cli": "npx shery-ui@latest add field",
        "manual": [
            {
                "title": "Install dependencies",
                "description": "Install the required dependencies for the field component.",
                "command": "npm install @radix-ui/react-label @radix-ui/react-separator class-variance-authority"
            },
            {
                "title": "Add utility function",
                "description": "Create a cn utility function if you don't have one already.",
                "filePath": "lib/utils.js",
                "code": "import { clsx } from 'clsx';\nimport { twMerge } from 'tailwind-merge';\n\nexport function cn(...inputs) {\n  return twMerge(clsx(inputs));\n}",
                "language": "javascript"
            }
        ]
    },
    "usage": {
        "import": "import { Field, FieldDescription, FieldGroup, FieldLabel } from '@shery-ui/components';",
        "basic": "field-demo"
    },
    "examples": [
        {
            "title": "Default",
            "description": "A basic field example.",
            "demo": "field-demo",
            "code": "import { Field, FieldDescription, FieldGroup, FieldLabel, Input } from \"@shery-ui/components\";\n\nexport default function FieldDemo() {\n  return (\n    <FieldGroup className=\"w-full max-w-sm\">\n      <Field>\n        <FieldLabel htmlFor=\"email\">Email</FieldLabel>\n        <Input id=\"email\" type=\"email\" placeholder=\"olivia@example.com\" />\n        <FieldDescription>Used for product and billing updates.</FieldDescription>\n      </Field>\n    </FieldGroup>\n  );\n}"
        }
    ]
}
